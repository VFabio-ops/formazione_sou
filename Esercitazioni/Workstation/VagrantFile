# -*- mode: ruby -*-
# vi: set ft=ruby :
Vagrant.configure("2") do |config|
  config.vm.box = "generic/rocky9"
  config.vm.provision "ansible", playbook: "provisioning.yml"
  config.vm.provider "virtualbox" do |vb|
    vb.memory = "2048"
    vb.cpus = 2
  end

  config.vm.define "rocky9" do |rocky9|
     rocky9.vm.hostname = "rocky9"
     rocky9.vm.network "private_network", ip: "192.168.100.2"
  end

end